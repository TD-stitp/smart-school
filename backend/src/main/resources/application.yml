spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/smartschool?useSSL=true&useUnicode=true&characterEncoding=utf8
    username: root
    password:
  thymeleaf:
    cache: false

server:
  port: 8080
